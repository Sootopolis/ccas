database = {
  dataSourceClassName = org.postgresql.ds.PGSimpleDataSource
  dataSource = {
    user = ccas
    password = ccas
    databaseName = ccas
    portNumber = 5432
    serverName = localhost
    currentSchema = test
  }
  connectionTimeout = 30000
}
