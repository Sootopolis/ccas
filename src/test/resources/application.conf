chess-com-client {
  contact-email = "test@test.com"
  permits = 5
  cooldown-seconds = 1
  failure-window-size = 20
  failure-threshold = 0.2
  min-sample-size = 10
  single-retry-delay-seconds = 1
}

database = {
  dataSource = {
    user = ccas
    password = ccas
    databaseName = ccas_test
    portNumber = 5432
    serverName = localhost
    currentSchema = public
  }
  pool = {
    maximumPoolSize   = 3
    minimumIdle       = 1
    connectionTimeout = 30000
  }
}
