database = {
  dataSource = {
    user = ccas
    password = ccas
    databaseName = ccas_test
    portNumber = 5432
    serverName = localhost
    currentSchema = public
  }
  pool = {
    maximumPoolSize   = 3
    minimumIdle       = 1
    connectionTimeout = 30000
  }
}
