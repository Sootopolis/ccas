database = {
  dataSource = {
    user = ccas
    password = ccas
    databaseName = ccas
    portNumber = 5432
    serverName = localhost
    currentSchema = test
  }
  pool = {
    maximumPoolSize   = 3
    minimumIdle       = 1
    connectionTimeout = 30000
  }
}
